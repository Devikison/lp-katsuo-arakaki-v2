/* ============================================================
   CONFIGURAÇÃO DA PÁGINA · edite apenas este arquivo
   ============================================================ */
window.LP_CONFIG = {
  // Número no formato internacional, só dígitos: 55 + DDD + número
  // >>> TROCAR pelo número real do Katsuo antes de publicar <<<
  whatsappNumber: "5515999999999",

  // Mensagem que já chega escrita no WhatsApp
  whatsappMessage: "Olá, Katsuo. Vim pela página e quero marcar a consulta de pré-terapia.",

  // Vídeo principal (VSL). Cole o link de embed do YouTube ou Vimeo.
  // Ex.: "https://www.youtube.com/embed/XXXXXXXX" ou "https://player.vimeo.com/video/000000"
  // Deixe vazio ("") enquanto não tiver o vídeo: a capa fica no lugar.
  vslUrl: "",

  // Vídeo de depoimento (cliente que passou 17 dias sem sintomas).
  // ATENÇÃO: antes de subir, cortar os trechos com a palavra "cura" (ficha do projeto).
  depoUrl: ""
};
